appId: com.storehub.salonx.staff.dev
tags:
  - leave-request
  - validation
  - boundary
  - P1
---
# LR-002: [边界] 申请日期与已有申请重叠
# Description: Submit a leave request with dates that overlap with existing request
# Expected: System shows error, prevents submission, prevents data conflict
# Risk Dimension: 变更风险 (Change Risk)

- launchApp:
    appId: "com.storehub.salonx.staff.dev"
    stopApp: true
    clearState: false
    permissions:
      all: "allow"

# Handle optional "Install Now" prompt
- runFlow:
    when:
      visible: 'Install Now'
    commands:
        - tapOn: "Install Now"

# Wait for app to load
- waitForAnimationToEnd

# Check if already logged in, if not, perform login
- runFlow:
    when:
      visible: "Enter your email"
    commands:
      # Login flow
      - tapOn: "Enter your email"
      - eraseText
      - inputText: "chad.deng+123@storehub.com"
      - tapOn: "Enter your password"
      - eraseText
      - inputText: 12345678
      - tapOn:
          text: "Sign In"
          index: 1
      - waitForAnimationToEnd
      # Handle store selection if it appears
      - runFlow:
          when:
            visible: "Salon.*"
          commands:
            - tapOn:
                text: "Salon.*"
                index: 0
            - tapOn: "Confirm"
            - waitForAnimationToEnd

# Navigate to Profile or Menu to find Leave Request
- runFlow:
    when:
      visible: "Profile"
    commands:
      - tapOn:
          point: "88%,95%"
      - waitForAnimationToEnd

# Navigate to Profile (Japanese)
- runFlow:
    when:
      visible: "個人"
    commands:
      - tapOn:
          point: "88%,95%"
      - waitForAnimationToEnd

# Look for Leave Request / Time Off option
- scrollUntilVisible:
    element:
      text: ".*[Ll]eave.*|.*[Tt]ime [Oo]ff.*|.*休暇.*"
    direction: "DOWN"

- tapOn:
    text: ".*[Ll]eave.*|.*[Tt]ime [Oo]ff.*|.*休暇.*"

- waitForAnimationToEnd

# Check if there's an existing leave request
# If not, create one first (prerequisite)
- runFlow:
    when:
      notVisible: ".*[Pp]ending.*|.*[Aa]pproved.*"
    commands:
      # Create a leave request first - tap + button (SVG icon)
      - tapOn:
          point: "92%,8%"
      - waitForAnimationToEnd
      # Enter reason
      - tapOn: "Enter reason for leave (optional)"
      - inputText: "First request"
      - waitForAnimationToEnd
      # Submit
      - tapOn: "Submit"
      - waitForAnimationToEnd

# Now try to create another request with overlapping dates
# Tap + button (SVG icon in top-right)
- tapOn:
    point: "92%,8%"

- waitForAnimationToEnd

# Form is pre-filled with today's date (same as existing request)
# This should create an overlapping request
# Add a reason
- tapOn: "Enter reason for leave (optional)"
- inputText: "Overlapping request test"

- waitForAnimationToEnd

# Try to submit the overlapping request
- tapOn: "Submit"

- waitForAnimationToEnd

# Verify error message appears
- assertVisible:
    text: ".*[Oo]verlap.*|.*[Cc]onflict.*|.*[Ee]xist.*|.*重复.*|.*冲突.*"

# Take screenshot of error
- takeScreenshot: overlapping-dates-error.png

# Verify submission is blocked (still on form or error shown)
- runFlow:
    when:
      visible: ".*[Ee]rror.*|.*[Ff]ailed.*"
    commands:
      - assertVisible:
          text: ".*[Ee]rror.*|.*[Ff]ailed.*"

